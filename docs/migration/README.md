# Historical Rust design

The [Rust migration plan](rust.md) records the original architecture and
acceptance ideas. Its implementation sequence and Python references are
historical; use the [crate map](../../crates/README.md) for implemented package
boundaries and the [operation docs](../operations/config-documents.md) for
current instance configuration.
